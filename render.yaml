services:
  - type: web
    name: hudorosu-backend
    env: node
    region: singapore
    plan: free
    rootDir: packages/backend
    buildCommand: cd ../.. && npm install --include=dev && npm run build:shared && npm run build:backend
    startCommand: npm run migrate && npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
